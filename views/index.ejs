<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>JS AUTOMATIZACIONES - Inicio</title>
  <link rel="stylesheet" href="./css/styles.css">
  <link rel="stylesheet" href="./css/responsive.css">
  <script src="https://kit.fontawesome.com/274f77f0e3.js" crossorigin="anonymous"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/png" href="./img/logo.png">
  	<meta name="theme-color" content="#5983a5">	
		<meta name="msapplication-navbutton-color" content="#5983a5">
		<meta name="apple-mobile-web-app-status-bar-style" content="#5983a5">
    <meta property="og:title" content="JS AUTOMATIZACIONES - Inicio">
    <meta property="og:description" content="Protege lo que más importa con nuestra tecnología de vanguardia y monitoreo profesional 24/7. Soluciones profesionales para la seguridad de tu hogar y empresa.">
    <meta property="og:image" content="https://blog.zequer.com/wp-content/uploads/2017/05/camaras-seguridad-895x530.jpg">
    <meta property="og:url" content="https://tusitio.com">
    <meta property="og:type" content="website">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="JS AUTOMATIZACIONES - Inicio">
    <meta name="twitter:description" content="Protege lo que más importa con nuestra tecnología de vanguardia y monitoreo profesional 24/7.">
    <meta name="twitter:image" content="https://blog.zequer.com/wp-content/uploads/2017/05/camaras-seguridad-895x530.jpg">
</head>
<body>
<div class="body-wrapper">
 <header>
    <div class="logodiv">
        <img src="./img/logo.png" alt="Logo JS AUTOMATIZACIONES" >
    </div>
  

  <!-- Botón Hamburguesa -->
  <button class="menu-toggle" id="menu-toggle">
    <i class="fa-solid fa-bars"></i>
  </button>

  <nav id="navbar">
    <a href="#products">Productos</a>
    <a href="#testimonials">Testimonios</a>
    <a href="#contact">Contacto</a>
  </nav>
</header>


  <main>
    <!-- Botón flotante WhatsApp -->
    <a href="https://wa.me/5492616604620" class="whatsapp-float" target="_blank" aria-label="Chat en WhatsApp">
      <i class="fa-brands fa-whatsapp"></i>
    </a>

    <!-- Hero -->
    <section class="hero bg-light">
      <div class="container hero">
        <div>
          <h1 class="text-primary">Seguridad Domiciliaria a tu Alcance</h1>
          <p>Protege lo que más importa con nuestra tecnología de vanguardia y monitoreo profesional 24/7.</p>
          <a href="#contact" class="btn btn-accent">Solicitar Presupuesto</a>
        </div>
        <img src="https://blog.zequer.com/wp-content/uploads/2017/05/camaras-seguridad-895x530.jpg" alt="Hero">
      </div>
    </section>

 
    <!-- Servicios -->
    <section id="services">
      <div class="container text-center">
        <h2>Nuestros Servicios</h2>
        <p>Soluciones profesionales para la seguridad de tu hogar y empresa.</p>
        <div class="services-grid">
          <% services.forEach(service => { %>
            <div class="card">
              <img src="<%= service.imageUrl %>" alt="<%= service.description %>">
              <div class="p-4">
                <h3><%= service.name %></h3>
                <p><%= service.description %></p>
              </div>
            </div>
          <% }) %>
        </div>
      </div>
    </section>

    <!-- Productos -->
    <section id="products">
      <div class="container text-center">
        <h2>Productos Destacados</h2>
        <p>Equipos y dispositivos de última tecnología para tu seguridad.</p>
        <div class="products-grid">
          <% products.forEach(product => { %>
            <div class="card">
              <img src="<%= product.imageUrl %>" alt="<%= product.description %>">
              <div class="p-4">
                <h3><%= product.name %></h3>
                <p><%= product.description %></p>
              </div>
            </div>
          <% }) %>
        </div>
      </div>
    </section>
    <!-- Galería -->
    <section id="gallery" class="bg-light">
      <div class="container text-center">
        <h2>Instalaciones Recientes</h2>
        <div class="gallery-grid">
          <% galeria.forEach(item => { %>
            <% if (item.type === "image") { %>
              <img src="<%= item.fileUrl %>" alt="<%= item.description %>">
            <% } else if (item.type === "video") { %>
              <video src="<%= item.fileUrl %>" controls></video>
            <% } else if (item.type === "audio") { %>
              <audio src="<%= item.fileUrl %>" controls></audio>
            <% } %>
          <% }) %>
        </div>
      </div>
    </section>


    <!-- Testimonios -->
    <section id="testimonials">
      <div class="container text-center">
        <h2>Lo que dicen nuestros clientes</h2>
        <div class="testimonials-grid">
          <% testimonials.forEach(testimonial => { %>
            <div class="card testimonial">
              <div class="p-4">
                <p>"<%= testimonial.quote %>"</p>
                <strong>- <%= testimonial.name %></strong>
              </div>
            </div>
          <% }) %>
        </div>
      </div>
    </section>

    <!-- Contacto -->
<section id="contact">
  <div class="container contact-grid">
    <div class="contact-info">
      <h2>Contáctenos</h2>
      <p>¿Tiene alguna pregunta o desea un presupuesto? Complete el formulario y nos pondremos en contacto con usted a la brevedad.</p>
      <p><strong>Dirección:</strong> San Martín Sur 7810, Mendoza, Argentina 5505</p>
      <p><strong>Instagram:</strong> <a href="#">@seguridaddomiciliaria</a></p>
    </div>

    <form id="contactForm" class="card p-4">
      <input type="text" name="name" placeholder="Nombre" required>
      <input type="email" name="email" placeholder="Correo" required>
      <textarea name="message" placeholder="Mensaje" rows="5" required></textarea>
      <button type="submit" class="btn btn-accent">Enviar</button>
    </form>
  </div>
</section>

<!-- Popup Modal -->
<div id="popup" class="popup">
  <div class="popup-content">
    <span id="closePopup" class="popup-close">&times;</span>
    <p id="popupMessage">Mensaje enviado correctamente</p>
  </div>
</div>

  </main>

  <footer>
    <p>&copy; 2025 JS AUTOMATIZACIONES. Todos los derechos reservados.</p>
    <div class="icons">
        <a href="https://www.instagram.com/seguridaddomiciliaria?igsh=dW55c2UzcnR0bnJm"><i class="fa-brands fa-instagram"></i></a>
        <a href="https://www.facebook.com/juan.safie.148"><i class="fa-brands fa-facebook"></i></a>
    </div>
    

  </footer>
</div>
  <script src="./js/script.js"></script>
<script>
  const form = document.getElementById("contactForm");
  const popup = document.getElementById("popup");
  const popupMessage = document.getElementById("popupMessage");
  const closePopup = document.getElementById("closePopup");

  form.addEventListener("submit", async (e) => {
    e.preventDefault();

    const formData = new FormData(form);
    const data = Object.fromEntries(formData.entries());

    try {
      const res = await fetch("/contact", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(data)
      });

      if (res.ok) {
        const result = await res.json();
        popupMessage.textContent = result.message;
        popup.style.display = "block";
        form.reset();
      } else {
        popupMessage.textContent = "Error enviando el mensaje. Intente nuevamente.";
        popup.style.display = "block";
      }
    } catch (err) {
      console.error(err);
      popupMessage.textContent = "Error enviando el mensaje. Intente nuevamente.";
      popup.style.display = "block";
    }
  });

  closePopup.onclick = () => {
    popup.style.display = "none";
  };

  window.onclick = (e) => {
    if (e.target == popup) {
      popup.style.display = "none";
    }
  };
</script>

</body>
</html>
